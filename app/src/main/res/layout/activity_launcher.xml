<!-- Customize this layout according to your requirements -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bottom_appbar_menu"
    android:gravity="center"
    tools:context=".ui.activity.LauncherActivity">

    <!-- Add your desired views, such as logo, progress bar, or introductory information -->
    <ImageView
        android:id="@+id/launcher_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_launcher"
        android:contentDescription="@string/logo" />

    <!-- Add any other views or layout elements as needed -->

</RelativeLayout>
